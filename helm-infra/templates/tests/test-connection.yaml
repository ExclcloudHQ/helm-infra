apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "heycoach-helm.fullname" . }}-test-connection"
  labels:
    {{- include "heycoach-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "heycoach-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
